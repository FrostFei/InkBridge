# InkBridge 开发执行说明

> 本文是交给 Codex 的产品需求、技术约束和实施指南。请根据本文在当前项目内完成可运行的软件，不要只输出方案或静态演示。
> 项目目录：F:\00-MyProject\InkBridge
> 需求整理日期：2026-09-06

## 1. 产品目标与使用场景

InkBridge（墨桥）是适配 iPad 的离线优先 PWA 笔记编辑器和 GitHub 同步客户端。用户在电脑继续使用 Obsidian，通过 Git / Obsidian Git 插件同步到 GitHub 私有仓库；在 iPad 使用 InkBridge 编辑同一个仓库。

主要解决两个问题：iPad 上同步 GitHub 笔记库麻烦；遇到文件冲突时缺少方便、可靠的处理界面。

核心优先级：数据不丢失 > 同步正确 > 冲突可解决 > 编辑体验 > 高级功能。不要把项目扩展成完整的 Obsidian 克隆。

边界：PWA 使用自己在浏览器中的本地副本，不直接读写 iPad Obsidian 的本地库，不是 Obsidian 插件，不承诺后台持续同步。不实现多人实时协作。

## 2. Codex 的执行方式

1. 先检查已有项目文件和适用的 AGENTS.md，保留已有有效实现，不盲目覆盖。
2. 创建简短实施计划，然后持续实现、运行和验证各阶段，不停在架构建议或待办清单。
3. 本文已确定的需求和普通实现细节自行决策；只对会改变数据安全、费用或产品范围的必要问题请求用户输入。
4. 没有真实 GitHub 凭据时，继续完成应用、模拟服务和自动化测试，最后列出真实仓库联调步骤。不要要求用户把 Token 发到聊天中。
5. 不修改用户真实笔记库来测试。真实写入验证使用用户明确指定的测试仓库；未获得授权时使用本地模拟。
6. 本文授权本地开发与构建，不代表授权公开部署、创建付费服务或修改生产笔记库。完成可审查产物后再处理这些操作。
7. 实际部署前重新核对所选平台官方文档与免费额度。不要把日期敏感的价格写成永久保证。

## 3. 第一版范围

必须实现：
- 响应式 PWA，可添加到 iPad 主屏幕；完成初次加载和离线准备后断网可打开。
- 连接 GitHub 仓库、选择已有分支、显示连接状态。
- 文件夹树、笔记新建/编辑/删除/重命名、Markdown 编辑与预览。
- 本地自动保存、所有支持的 Markdown 笔记全文下载、离线搜索。
- 常用 Obsidian 语法：相对链接、[[笔记]]、[[笔记|别名]]、标题锚点、标签、YAML frontmatter 保留、常用图片嵌入。
- 手动同步、进入应用/恢复前台时检查更新、恢复网络后的同步尝试。
- 三方文本合并、可交互的冲突解决、远端并发修改保护。
- 图片/PDF 等附件列表、按需下载、显式全量离线下载；未下载时显示离线不可用。
- 导出当前本地笔记和已下载附件为 ZIP；准确说明未包含哪些未下载附件。
- 清晰区分本地保存、云端同步和离线准备状态。

暂不实现：关系图谱、第三方插件运行时、Dataview 执行、完整 Canvas 编辑、AI、手写、PDF 批注、多人实时协作和付费账号体系。

## 4. 推荐技术方案

若目录为空，默认使用 TypeScript、React、Vite、CodeMirror 6、IndexedDB（可用 Dexie）、成熟 Markdown 渲染器和 Service Worker / PWA 插件。测试使用 Vitest 与 Playwright。根据实现需要选择维护良好的三方文本合并、ZIP 和解析库，不自行发明 Git 合并算法。

默认纯静态前端，通过 HTTPS 调用 GitHub API，不依赖自建服务器或数据库。首选 Cloudflare Pages，保持可部署到其他静态托管平台。应用代码仓库与私有笔记仓库分开；构建产物中不得包含真实笔记或凭据。

本地保存笔记当前内容、共同基准和同步元数据，不要求下载完整 Git 历史。同步引擎与界面、存储层、GitHub API 层分离，可在无 UI、无真实网络环境下测试。

## 5. 本地存储与自动保存

建议数据模型至少包含：
- 仓库标识、分支、最近完整同步的 commit SHA、离线下载进度。
- 文件路径、类型、base blob SHA/base 内容、当前本地内容、本地 revision、脏状态、删除标记。
- 待处理操作及持久化同步事务、远端快照引用、未解决冲突。
- 附件下载状态和缓存；可重建的搜索、标签和反向链接索引。

要求：
- 短防抖后持久化编辑；只有 IndexedDB 事务成功后显示“本地已保存”。不要只依赖页面关闭事件保存。
- 本地保存失败、空间不足时明确报错，不得仍然显示保存成功。
- 保存单调递增的本地 revision。同步期间用户继续输入时，提交只对应已捕获快照；确认提交后只清除对应 revision 的脏状态，保留后续编辑。
- 同步事务和冲突持久化，刷新或崩溃后可恢复。
- 申请持久化存储并展示结果，但不能声称浏览器数据永不会被清理。
- 展示存储使用估计、全部笔记下载进度、未下载附件和待同步数量。
- 切换仓库/分支严格隔离数据；存在未同步修改时保留本地副本并明确提示。

## 6. GitHub 连接与授权

第一版面向个人用户，使用细粒度 Personal Access Token，仅授权选定笔记仓库的必要内容读写权限。默认 Token 仅在当前会话内存中保留，刷新后重新输入；离线编辑不依赖 Token。

禁止：将 Token 写入源码、URL、日志、笔记仓库、构建环境中的前端公开变量或导出文件。不要为了方便硬编码凭据。未来如增加持久化凭据方案，需单独评估安全与使用体验。

提供仓库、分支、权限校验与可理解的错误信息，区分凭据过期、权限不足、网络错误、限流、分支保护和仓库不存在。正确处理分页、截断的目录树、二进制内容和中文/空格/特殊字符路径。无法完整读取时不得将其误认为远端删除。

分支保护或强制签名限制导致写入失败时显示原因并保留本地修改，不自动禁用保护、不强制推送。

## 7. 同步算法：最高优先级

三份输入：B = 上次确认同步的共同基准；L = 捕获的本地快照；R = 本次拉取的远端最新快照。

首次连接：完整获取支持范围内的当前文件内容后建立基准，并明确展示完成程度；不下载完整历史。首次下载被中断可以恢复，尚未下载的文件不得按空文件或删除处理。

每次同步：
1. 获取同一仓库/分支的同步锁，禁止多个同步请求或多个浏览器标签同时提交；UI 编辑仍可进行。
2. 持久化事务，并捕获 L 及每个文件的 revision。
3. 读取远端 head H 与该 head 下的完整相关文件状态 R。
4. 根据 B/L/R 计算新增、修改、删除和冲突；共同基准缺失时不能猜测，转入显式恢复流程。
5. 对互不重叠的文本修改自动合并；重叠修改进入冲突界面，不以最后写入覆盖。
6. 第一版若存在冲突，暂停本次远端提交，让用户处理完后继续。原始 B/L/R 保持可恢复。
7. 基于 H 的完整 tree 创建 blobs/tree/单个 commit；保留未编辑的文件和 tree 项。更新 branch ref 必须为非强制更新，禁止 force push。
8. 若远端在此期间前进，非快进更新应失败；重新获取远端并重新计算合并。设置有限重试，超过次数提示稍后重试。
9. 确认远端提交结果后，在本地事务中更新共同基准，并仅确认该次已提交 revision。若用户期间继续编辑，以提交快照为新基准继续保留差异。

优先使用 Git Database API 完成多文件原子提交，避免逐文件 Contents API 提交导致重命名、删除和修改只完成一半。

网络超时不能直接等同于提交失败：先查询远端确认该 commit 是否已被接纳，避免重复提交或错误回滚。没有差异时不创建空提交。

远端历史被重写时，不依赖强制推送恢复；保留本地副本，重新获取远端，并在可证明共同基准时合并，否则提供人工恢复。不要自动覆盖远端历史。

## 8. 冲突处理界面和规则

冲突列表显示路径、类型和来源。iPad 横屏可并排查看本地/远端并编辑结果；竖屏用切换标签，保证触控可操作。支持查看共同基准。

每个冲突提供：使用 iPad 版本、使用 GitHub 版本、保留两份（选择新文件名）、手动编辑合并结果。保存解决结果后标记已解决；未经明确选择不得丢弃任一版本。不要将冲突标记直接写进正常笔记。

必须覆盖：
- 同段文字双方修改。
- 双方新增同名但不同内容的文件。
- 一端删除、另一端修改：明确选择删除或恢复修改内容。
- 同路径附件内容不同：选择版本或保留两份，不执行文本合并。
- 重命名与编辑交叉：Git 没有原生重命名记录，识别不明确时按关联的删除/新增操作展示并让用户确认，不能声称总能自动识别。

用户处理冲突期间 L 或 R 再次变化，要重新校验并保留人工合并草稿；不能将过期解决结果直接上传。

## 9. 与 Obsidian 的兼容性

- 保持现有文件结构、中文文件名、大小写和附件相对路径，不强制迁移到 notes/。
- Markdown 是真实数据源，索引可以重建。未编辑笔记不做格式化或换行转换；保留 frontmatter 和不支持的语法。
- 支持常见 wiki 链接和嵌入；同名目标存在歧义时显示选择，不静默跳到任意笔记。
- .obsidian/ 默认不由网页版编辑或同步本地工作区状态，但创建远端 tree 时完整保留；网页自己的配置与缓存不污染现有 Obsidian 配置。
- Canvas、插件数据和其他未支持文件不删除、不改写；列明哪些可预览、哪些仅保留在远端。不要声称完整兼容 Obsidian 插件。
- 重命名笔记时提示受影响链接；如果提供更新链接，只修改能够可靠解析的链接，和文件重命名一起原子提交。
- 电脑端文档说明安装 Git、配置 Obsidian Git 或其他 Git 客户端；切换设备前完成推送，开始编辑前拉取。仍需保证未遵循此建议时冲突不会导致静默丢失。

## 10. 交互与安全

界面使用简体中文，产品名 InkBridge。默认简洁浅色/深色主题，适配 iPad 触屏、软键盘和外接键盘。主视图为文件导航、编辑器、可折叠预览/引用面板，提供显眼的同步按钮。

状态文案至少区分：正在本地保存、本地已保存、待上传 N 项、正在同步、已同步、冲突待处理、同步失败、离线可编辑。不得用单个“已保存”暗示上传成功。

将笔记内容视为不可信输入：Markdown HTML 清洗，禁止脚本执行与危险 URL；外部图片要可控，私有附件携带授权下载后使用本地 Blob URL。不得让笔记中的 HTML 读取凭据。设置合理 CSP，避免不必要第三方脚本。

Service Worker 只缓存适当的应用资源；不得把带凭据的 API 请求盲目加入通用缓存。新版本更新不能在存在未保存内容时强制刷新。

## 11. 实施里程碑

M1：项目骨架、响应式编辑器、IndexedDB 自动保存、刷新恢复和基础测试。
M2：GitHub 只读连接、笔记全量下载、搜索和 PWA 离线打开。
M3：版本模型、增量同步、原子提交和并发保护。
M4：自动合并、所有必需冲突类型的交互处理与恢复。
M5：附件、常用 Obsidian 语法、导出、部署文档和端到端验证。

每阶段产出可运行增量。实现同步前先用模拟 GitHub 服务建立冲突测试样例。不要用假按钮、延时动画或内存假数据冒充已完成的真实能力。

## 12. 验收标准与测试

自动化测试至少覆盖：
1. 编辑并确认本地保存后刷新，内容仍在；断网可读取已下载笔记并创建新笔记。
2. 只有本地变化/只有远端变化/双方相同变化，均得到正确结果。
3. 双方修改不同段落可合并；修改同段落产生可恢复冲突。
4. 同名新增、删除对修改、二进制冲突均不静默覆盖。
5. 提交前远端出现新 commit，拒绝旧快照写入并重新合并。
6. 多文件变更及重命名在远端以单个提交完成，未支持文件和 .obsidian/ 不丢失。
7. API 超时、权限失效、限流、树截断、附件失败不会清空本地数据或误判删除。
8. 同步期间继续编辑、重复点击同步、两个标签同时操作，最新输入不被确认逻辑清除。
9. 冲突处理中刷新，以及提交成功后本地确认前崩溃，能够恢复。
10. 中文/空格路径、大小写不同文件名、换行差异与同名链接歧义处理正确。
11. ZIP 导出可解压并与实际本地内容一致，不包含 Token。
12. 恶意 Markdown 不能执行脚本或窃取凭据。

运行类型检查、生产构建、针对同步逻辑的单元测试和核心端到端测试。桌面浏览器的 iPad 视口模拟不等于真机验证；没有真实 iPad 时明确标注未验证，并提供 Safari 真机验收清单（添加主屏幕、离线启动、软键盘、存储、附件下载和恢复前台同步）。

## 13. 交付要求

交付源代码、依赖锁文件、可运行开发命令、构建与测试命令、README、Cloudflare Pages 部署步骤、GitHub Token 最小权限配置指南、电脑 Obsidian 接入指南、同步设计说明和已知限制。

最终汇报实际实现的功能、测试结果、未完成项及真实联调需求。不将模拟通过描述成真实 GitHub 或 iPad 验证通过。

验收成功的核心场景：电脑 Obsidian 与 iPad InkBridge 对同一私有笔记库分别编辑，在存在冲突时用户能在 iPad 上完成选择或合并，随后两端收敛到一致内容，过程中没有静默丢失任何未确认的修改。

## 14. 官方与项目参考资料

实施时按需核对最新文档：
- GitHub Git Database API：https://docs.github.com/en/rest/git
- GitHub Contents API：https://docs.github.com/en/rest/repos/contents
- GitHub Token 管理：https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens
- Cloudflare Pages：https://developers.cloudflare.com/pages/
- WebKit 存储机制：https://webkit.org/blog/14403/updates-to-storage-policy/
- Obsidian Git 插件文档：https://publish.obsidian.md/git-doc/
