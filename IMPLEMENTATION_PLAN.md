# InkBridge 实施记录

依据 DEVELOPMENT_SPEC.md。初始检查：目录只有规范，无现有实现、Git 仓库或适用 AGENTS.md。

- M1：React / TypeScript / Vite 骨架、响应式编辑器、Dexie 自动保存和刷新恢复。
- M2：GitHub API 只读连接、完整快照、笔记全文下载、搜索和 PWA 预缓存。
- M3：持久化版本与事务、Web Locks 跨标签互斥、Git Database 原子提交、非强制更新与恢复。
- M4：成熟三方合并库、交互冲突处理、草稿和崩溃恢复。
- M5：Obsidian 常用语法、附件离线下载、ZIP 导出、部署和真机验收文档。

## 阶段产出

| 阶段 | 实际实现与验证                                                                                    |
| ---- | ------------------------------------------------------------------------------------------------- |
| M1   | React / CodeMirror 响应式界面、Dexie 保存与刷新恢复、revision 和存储失败处理；浏览器编辑/重载验证 |
| M2   | 细粒度 Token 会话连接、已有分支选择、完整快照、SHA 校验及续传缓存、全文搜索、PWA 离线打开         |
| M3   | 先建立模拟 GitRemote 用例，再实现独立引擎；多文件单提交、Web Locks、远端并发与崩溃确认恢复        |
| M4   | 成熟三方合并、可恢复 B/L/R 草稿、所有必需冲突选择、晚到编辑再次合并、模糊重命名关联提示           |
| M5   | 附件、常见 Obsidian 语法、ZIP 遗漏报告、中文文档、安全头、CI 和端到端测试                         |

执行中修复了首次选择与 IndexedDB 异步刷新竞态、提交后确认错误清除后续差异的风险、过期事务占用空间、深色主题对比度和编辑器切换初始化。详细测试结果与未执行的真实联调见 `docs/VALIDATION.md`。

验证：模拟 GitHub 服务覆盖并发、失败和冲突；执行类型检查、单元测试、生产构建和 Playwright 核心流程。未使用真实 Token、未写入真实笔记库、未公开部署。
